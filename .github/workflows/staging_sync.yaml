# Disabled in fork — this workflow is for Teamwork internal staging infrastructure only.
# See: https://github.com/Teamwork/mcp for the upstream version.
name: Staging sync (disabled)
on:
  workflow_dispatch:
    inputs:
      noop:
        description: 'This workflow is disabled in the fork'
        required: false
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Staging sync is disabled in this fork"
